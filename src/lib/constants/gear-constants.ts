import type { GearType } from "~/types/gear";

// User-friendly labels for gear types
// Used across the application for consistent display of gear types
export const GEAR_TYPE_LABELS: Record<GearType, string> = {
  CAMERA: "Cameras",
  ANALOG_CAMERA: "Analog Cameras",
  LENS: "Lenses",
};
